#pragma once

// C++ macros to allow clean schema definitions without macro expansion side-effects
#define db_primary_key
#define db_index
#define db_unique
#define col_type(t)
#define db_one2many(t)
